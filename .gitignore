node_modules
.browser_modules
lib
*.log
*-app/*
!*-app/package.json
!electron-app/product.json

# captures de design (locales)
tools/shots/

# checkout de comparaison swarm-engine (fork + upstream) — local uniquement
_cmp/
