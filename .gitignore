node_modules
.browser_modules
lib
*.log
*-app/*
!*-app/package.json
!electron-app/product.json
!electron-app/electron-builder.yml
!electron-app/resources/
!electron-app/resources/**

# captures de design (locales)
tools/shots/

# checkout de comparaison swarm-engine (fork + upstream) — local uniquement
_cmp/

# Dépôts de travail isolés utilisés pour auditer/intégrer le moteur, le CLI et le registry.
audit/
